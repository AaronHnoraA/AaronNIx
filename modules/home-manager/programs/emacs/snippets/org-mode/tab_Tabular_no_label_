# -*- mode: snippet -*-
# name: Tabular (no label)
# key: tab
# -- Tabular without label
# --
\begin{tabular}{${1:c}}
$0
\end{tabular} `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`