# -*- mode: snippet -*-
# name: Sub Sub Section (with label)
# key: subsl
# -- Sub Sub Section with label
# --
\subsubsection{$1}\label{sec:$1}
${0:$TM_SELECTED_TEXT} `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`