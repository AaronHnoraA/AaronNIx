# -*- mode: snippet -*-
# name: Split
# key: spl
# -- Split
# --
\begin{split}
	$0
\end{split} `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`