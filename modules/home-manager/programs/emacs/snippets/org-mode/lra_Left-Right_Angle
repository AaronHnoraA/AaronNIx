# -*- mode: snippet -*-
# name: Left-Right Angle
# key: lra
# --
\left< $1 \right> $2 `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`