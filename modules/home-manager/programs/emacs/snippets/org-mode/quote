# -*- mode: snippet -*-
# name: qu
# key: qu
# --
#+begin_quote
注意：这是一个特殊的说明
#+end_quote `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`