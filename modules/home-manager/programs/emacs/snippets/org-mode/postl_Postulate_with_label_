# -*- mode: snippet -*-
# name: Postulate (with label)
# key: postl
# -- Add a postulate with label
# --
\begin{postulate}{$1}\label{pos:$1}
		$2
\end{postulate}
$0 `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`