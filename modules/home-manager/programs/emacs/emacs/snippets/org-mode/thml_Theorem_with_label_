# -*- mode: snippet -*-
# name: Theorem (with label)
# key: thml
# -- Add a theorem with label
# --
\begin{theorem}{$1}\label{thm:$1}
	$2
\end{theorem}
$0 `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`