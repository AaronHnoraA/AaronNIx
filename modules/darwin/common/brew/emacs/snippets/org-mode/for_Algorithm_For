# -*- mode: snippet -*-
# name: Algorithm:For
# key: for
# --
\For{i=0:$1}
	\State $0
\EndFor `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`