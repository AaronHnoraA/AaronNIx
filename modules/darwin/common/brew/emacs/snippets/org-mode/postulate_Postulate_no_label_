# -*- mode: snippet -*-
# name: Postulate (no label)
# key: postulate
# -- Add a postulate without label
# --
\begin{postulate}{$1}
		$2
\end{postulate}
$0 `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`