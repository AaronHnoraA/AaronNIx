# -*- mode: snippet -*-
# name: Lemma (with label)
# key: lemmal
# -- Add a lemma with label
# --
\begin{lemma}{$1}\label{lem:$1}
	$2
\end{lemma}
$0 `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`