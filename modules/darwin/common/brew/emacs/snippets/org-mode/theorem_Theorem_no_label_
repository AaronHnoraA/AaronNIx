# -*- mode: snippet -*-
# name: Theorem (no label)
# key: theorem
# -- Add a theorem without label
# --
\begin{theorem}{$1}
	$2
\end{theorem}
$0 `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`