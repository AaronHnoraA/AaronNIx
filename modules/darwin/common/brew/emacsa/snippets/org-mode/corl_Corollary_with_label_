# -*- mode: snippet -*-
# name: Corollary (with label)
# key: corl
# -- Add a corollary with label
# --
\begin{corollary}{$1}\label{cor:$1}
	$2
\end{corollary}
$0 `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`